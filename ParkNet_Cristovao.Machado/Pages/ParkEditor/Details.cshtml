@page
@model ParkNet_Cristovao.Machado.Pages.ParkEditor.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Park</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Park.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Park.Name)
        </dd>
        <p></p>
        <dt class="col-sm-2">
            Layout:
        </dt>
        <dd class="col-sm-10">
            <div style="overflow-x:auto;"></div>
<pre>
                @for (int i = 0; i < Model.Layout.GetLength(0); i++)
                {
                    for (int j = 0; j < Model.Layout.GetLength(1); j++)
                    {
                        @Model.Layout[i, j]
                    }
                <br />
                }
</pre>
</div>
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Park.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
